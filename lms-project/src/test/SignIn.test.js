import { render , screen } from '@testing-library/react'
import SignIn from '../component/pages/SignIn'


describe('testing the signin component',()=>{
      test('testing the names texts are render correct',()=>{
            // render(<SignIn />)
             // expect(screen.getByText(/Elevate your learning experience with our LMS portal./i)).toBeInTheDocument();     
       })
})